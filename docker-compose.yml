version: '3.8'
services:
  nasa-yuzaki-bot:
    image: node:18-alpine
    container_name: nasa-yuzaki-bot
    restart: unless-stopped
    working_dir: /app
    volumes:
      - ./:/app
    command: npm start
    env_file:
      - .env
